const initialState = [
    "TITLE",
    "CLARIFICATION",
    "ILLUSTRATIVE_IMAGE",
];

const allContentBlockTypeIds = ( currentState = initialState, action ) =>
{
    switch ( action.type )
    {
        default:
            return currentState;
    }
};


export default allContentBlockTypeIds;